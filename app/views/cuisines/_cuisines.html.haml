-@cuisines.each do |cuisine|
  %p
    =cuisine.name
    %span{ :style => 'float: right;'}
      =link_to 'View', cuisine_path(cuisine)
      |
      =link_to 'Edit', edit_cuisine_path(cuisine)
      |
      =link_to 'Delete', cuisine, :method => 'delete', :confirm => 'Are you sure?'
  %hr